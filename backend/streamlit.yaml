Build Command: pip install -r backend/requirements.txt

Start Command: streamlit run backend/app/streamlit.py --server.port $PORT --server.address 0.0.0.0

Add Environment Variable".

Key: BACKEND_URL    value: https://edge-optimized-real-time-vehicle.onrender.com

PYTHON_VERSION    value: 3.10.12
